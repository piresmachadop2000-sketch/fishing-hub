import Link from 'next/link';
import { Header } from '@/components/Header';
import { experiences } from '@/lib/data';
export default function Experiencia({params}:{params:{id:string}}){const exp=experiences.find(e=>e.id===params.id)||experiences[0];return <><Header/><main className="page"><div className="detailHero" style={{backgroundImage:`url(${exp.image})`}}><div className="overlay"><div><div className="chips">{exp.badges.map(b=><span className="chip green" key={b}>{b}</span>)}</div><h1>{exp.title}</h1></div></div></div><div className="detailGrid"><section className="panel"><div className="row space"><strong>{exp.location} • {exp.region}</strong><span>⭐ {exp.rating} ({exp.reviews})</span></div><p className="muted">{exp.description}</p><div className="tabs"><span className="tab">{exp.type}</span><span className="tab">{exp.duration}</span><span className="tab">{exp.capacity}</span><span className="tab">{exp.species.join(', ')}</span></div><h2>O que está incluso</h2><div className="list">{exp.includes.map(i=><div className="listItem" key={i}>✅ {i}</div>)}</div><h2>O que não está incluso</h2><div className="list">{exp.notIncludes.map(i=><div className="listItem" key={i}>— {i}</div>)}</div><h2>Regras e segurança</h2><p>Política de reagendamento em caso de clima severo. Uso obrigatório de colete. Pesca esportiva recomendada. Ponto de encontro confirmado após reserva.</p><h2>Avaliações</h2><div className="grid2"><div className="panel"><strong>Conhecimento do guia</strong><p>5.0/5.0</p></div><div className="panel"><strong>Estrutura</strong><p>4.9/5.0</p></div></div></section><aside className="booking"><h2>R$ {exp.price.toLocaleString('pt-BR')}</h2><p className="muted">por experiência</p><input type="date"/><select><option>1 pescador</option><option>2 pescadores</option><option>3 pescadores</option></select><select><option>Dia inteiro</option><option>Meio período</option><option>Pacote personalizado</option></select><Link className="button" href={`/reservar/${exp.id}`} style={{display:'block',textAlign:'center',marginTop:12}}>Reservar agora</Link><Link className="button ghost" href="/chat" style={{display:'block',textAlign:'center',marginTop:10}}>Falar com o guia</Link><hr/><p><strong>Guia:</strong> <Link href={`/guia/${exp.guideId}`}>{exp.guide}</Link></p></aside></div></main></>}
