import { Header } from '@/components/Header';
import { ExperienceCard } from '@/components/Card';
import { experiences, guide } from '@/lib/data';
export default function Guia(){return <><Header/><main className="page"><section className="panel"><div className="row space"><div><h1>{guide.name}</h1><p className="muted">{guide.location} • ⭐ {guide.rating} ({guide.reviews}) • {guide.response}</p></div><div className="chips">{guide.badges.map(b=><span className="chip green" key={b}>{b}</span>)}</div></div><p>{guide.bio}</p><div className="statGrid"><div className="stat"><b>{guide.trips}</b><p>Pescarias</p></div><div className="stat"><b>{guide.rating}</b><p>Nota média</p></div><div className="stat"><b>{guide.since}</b><p>Na atividade</p></div><div className="stat"><b>98%</b><p>Resposta</p></div></div><h2>Especialidades</h2><div className="chips">{guide.specialties.map(s=><span className="chip" key={s}>{s}</span>)}</div></section><section className="section"><h2>Experiências deste guia</h2><div className="grid">{experiences.slice(0,1).map(e=><ExperienceCard exp={e} key={e.id}/>)}</div></section></main></>}
